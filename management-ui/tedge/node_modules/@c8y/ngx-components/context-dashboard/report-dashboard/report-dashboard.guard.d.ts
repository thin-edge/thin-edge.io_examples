import { ActivatedRouteSnapshot } from '@angular/router';
import { ContextDashboardService } from '../context-dashboard.service';
export declare class ReportDashboardGuard {
    private contextDashboardService;
    constructor(contextDashboardService: ContextDashboardService);
    canActivate(route: ActivatedRouteSnapshot): import("rxjs").Observable<boolean | {
        icon: string;
        path: string;
        label: string;
        priority: number;
        hide: any;
    }[]>;
    createDashboardIfNotExist(reportId: any): Promise<boolean>;
}
