import { PasswordService } from '../authentication';
export declare class StrengthValidatorService {
    private passwordService;
    constructor(passwordService: PasswordService);
    isStrong(password: string): boolean;
    private isPasswordGreen;
}
