import { ConfigOption, FieldType } from '@ngx-formly/core';
export declare class FieldRadio extends FieldType {
    static readonly CONFIG: ConfigOption;
    defaultOptions: {
        templateOptions: {
            options: any[];
            formCheck: string;
        };
    };
}
