import { ConfigOption, FieldType } from '@ngx-formly/core';
export declare class FieldCheckbox extends FieldType {
    static readonly CONFIG: ConfigOption;
    defaultOptions: {
        templateOptions: {
            indeterminate: boolean;
            formCheck: string;
        };
    };
}
