import { NavigatorNode, NavigatorNodeFactory } from '@c8y/ngx-components';
import { Observable } from 'rxjs';
import { AbstractConnectivityTabFactory } from './abstract-connectivity-tab.factory';
export declare abstract class AbstractConnectivityNodeFactory implements NavigatorNodeFactory {
    protected tabFactory: AbstractConnectivityTabFactory;
    protected readonly connectivityNode: NavigatorNode;
    constructor(tabFactory: AbstractConnectivityTabFactory);
    get(): Observable<NavigatorNode[]>;
    protected wrapIntoObservable<T>(value: T | Observable<T>): Observable<T>;
}
