import { Router } from '@angular/router';
import { Tab, TabFactory } from '@c8y/ngx-components';
import { Observable } from 'rxjs';
import { ImpactGuard } from './impact.guard';
export declare class ImpactTabFactory implements TabFactory {
    private router;
    private guard;
    private config;
    constructor(router: Router, guard: ImpactGuard);
    get(): any[] | Tab | Observable<Tab>;
    tab(): Tab | Observable<Tab>;
    private isPromise;
    private wrapIntoObservable;
}
