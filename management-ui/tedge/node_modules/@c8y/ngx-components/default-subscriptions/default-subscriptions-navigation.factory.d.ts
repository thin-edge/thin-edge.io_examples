import { NavigatorNode, NavigatorNodeFactory } from '@c8y/ngx-components';
import { DefaultSubscriptionsGuard } from './default-subscriptions.guard';
export declare class DefaultSubscriptionsNavigationFactory implements NavigatorNodeFactory {
    private defaultSubscriptionsGuard;
    private navNode;
    constructor(defaultSubscriptionsGuard: DefaultSubscriptionsGuard);
    /** Returns the navigation node if the feature is active. */
    get(): Promise<NavigatorNode>;
}
