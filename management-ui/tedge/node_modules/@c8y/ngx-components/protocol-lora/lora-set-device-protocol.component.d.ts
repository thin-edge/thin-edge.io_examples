import { OnInit } from '@angular/core';
import { LoraSetDeviceProtocolService } from './lora-set-device-protocol.service';
import { AlertService } from '@c8y/ngx-components';
import { IManagedObject, InventoryService, IResultList } from '@c8y/client';
import { Router } from '@angular/router';
export declare class LoraAssignDeviceProtocolComponent implements OnInit {
    private loraService;
    private alertService;
    private router;
    private inventoryService;
    loading: boolean;
    device: IManagedObject;
    currentProtocol: IManagedObject;
    availableProtocols: IResultList<IManagedObject>;
    newProtocol: IManagedObject;
    filterCurrentProtocol: import("rxjs").UnaryFunction<import("rxjs").Observable<IManagedObject[]>, import("rxjs").Observable<IManagedObject[]>>;
    loraSetDeviceProtocolForm: any;
    constructor(loraService: LoraSetDeviceProtocolService, alertService: AlertService, router: Router, inventoryService: InventoryService);
    ngOnInit(): Promise<void>;
    reload(): Promise<void>;
    loadDevice(): Promise<void>;
    apply(selectedProtocol: any): Promise<void>;
}
